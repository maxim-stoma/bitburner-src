<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Главная](./index.md) &gt; [bitburner](./bitburner.md) &gt; [NS](./bitburner.ns.md) &gt; [flags](./bitburner.ns.flags.md)

## NS.flags() method

Разобрать флаги командной строки.

**Сигнатура:**

```typescript
flags(schema: [string, any][]): {
    [key: string]: any;
    _: ScriptArg[];
  };
```

## Параметры

<table><thead><tr><th>

Параметр


</th><th>

Тип


</th><th>

Описание


</th></tr></thead>
<tbody><tr><td>

schema


</td><td>

\[string, any\]\[\]


</td><td>


</td></tr>
</tbody></table>

**Возвращает:**

{ \[key: string\]: any; \_: [ScriptArg](./bitburner.scriptarg.md)<!-- -->\[\]; }

## Примечания

Расход RAM: 0 GB

Позволяет разбирать флаги в стиле Unix.

Поддерживаются 2 формы:

- Короткая форма: флаг из 1 символа, например -v.

- Длинная форма: флаг длиннее 1 символа, например --version.

Учтите: если аргумент передан, а его значение по умолчанию — nullish, разобранное значение будет строкой. Это может привести к неочевидным проблемам, если не следить за приведением типов.

## Пример

```js
export async function main(ns) {
  const data = ns.flags([
    ['delay', 0], // a default number means this flag is a number
    ['server', 'foodnstuff'], //  a default string means this flag is a string
    ['exclude', []], // a default array means this flag is a default array of string
    ['help', false], // a default boolean means this flag is a boolean
    ['v', false], // short form
  ]);
  ns.tprint(data);
}

// [home /]> run example.js
// {"_":[],"delay":0,"server":"foodnstuff","exclude":[],"help":false,"v":false}
// [home /]> run example.js --delay 3000
// {"_":[],"delay":3000,"server":"foodnstuff","exclude":[],"help":false,"v":false}
// [home /]> run example.js --delay 3000 --server harakiri-sushi
// {"_":[],"delay":3000,"server":"harakiri-sushi","exclude":[],"help":false,"v":false}
// [home /]> run example.js --delay 3000 --server harakiri-sushi hello world
// {"_":["hello","world"],"delay":3000,"server":"harakiri-sushi","exclude":[],"help":false,"v":false}
// [home /]> run example.js --delay 3000 --server harakiri-sushi hello world --exclude a --exclude b
// {"_":["hello","world"],"delay":3000,"server":"harakiri-sushi","exclude":["a","b"],"help":false,"v":false}
// [home /]> run example.js --help
// {"_":[],"delay":0,"server":"foodnstuff","exclude":[],"help":true,"v":false}
// [home /]> run example.js -v
// {"_":[],"delay":0,"server":"foodnstuff","exclude":[],"help":false,"v":true}
```

```js
export async function main(ns) {
  const data = ns.flags([
    ["foo", null],
    ["bar", undefined],
  ]);
  console.log(data);
}

// [home /]> run example.js
// { _: [], foo: null, bar: undefined }
// [home /]> run example.js --foo 1000
// { _: [], foo: "1000", bar: undefined }
// [home /]> run example.js --foo 1000 --bar false
// { _: [], foo: "1000", bar: "false" }
```
`bar` в последнем примере — `"false"` (строка), а не `false` (булево значение). `data.bar` — truthy, а не falsy.
